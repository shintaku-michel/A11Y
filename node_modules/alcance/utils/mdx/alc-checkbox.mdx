
## Propriedades

| Nome | Descrição | Tipo | Default |
| ---- | --------- | ---- | ------- |
| `error-msg`  | Texto de mensagem de erro. | <code>string</code> | `''` |
| `hint`  | Texto de ajuda. | <code>string</code> | `''` |
| `label`  | Texto do label do input. Há opção de adicionar o elemento label pelo slot "label". | <code>string</code> | `''` |

*Aprenda mais sobre <a href="/alcance/docs/components/introducao#propriedades" class="alc-link">propriedades</a>.*

## Slots

| Nome | Descrição |
| ---- | --------- |
| `DEFAULT` | Slot para o elemento input do tipo checkbox. |
| `label` | Slot para o elemento label do checkbox. Há opção de adicionar apenas o texto por meio da propriedade `label`. |

*Aprenda mais sobre <a href="/alcance/docs/components/introducao#slot" class="alc-link">slot</a>.*
