import { setMode } from '@stencil/core/internal/client';
export { getAssetPath, setAssetPath, setNonce, setPlatformOptions } from '@stencil/core/internal/client';
import { l as loadUserPreference, s as setAppliedTheme, g as getAppliedTheme, a as getMode } from './theme.js';

var appGlobalScript = () => {
    setMode(() => getMode());
};
// Ajuste inicial do tema
const userPreference = loadUserPreference();
setAppliedTheme(getAppliedTheme(userPreference));

const globalScripts = appGlobalScript;

globalScripts();
//# sourceMappingURL=index.js.map

//# sourceMappingURL=index.js.map