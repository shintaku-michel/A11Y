import { Build } from "@stencil/core";
export default function testAttributes(attributes) {
    let result = {};
    if (Build.isDev || Build.isTesting) {
        attributes.split(' ').forEach((attr) => result[attr] = true);
    }
    return result;
}
//# sourceMappingURL=testAttributes.js.map
