import { EventEmitter } from '../../stencil-public-runtime';
/**
 * @slot DEFAULT - Slot para skip-links, identificação do ambiente, etc.
 * @slot support - Slot para o conteúdo de apoio, como links de ajuda, contato etc.
 * @slot fixed - Slot para conteúdo fixo, como botões de ações rápidas.
 * @slot user - Slot para o menu de usuário.
 */
export declare class AlcHeader {
    private navPanel;
    private slotSupport;
    private slotFixed;
    private slotUser;
    private supportItems;
    private userMenu;
    private supportWrapper;
    private support;
    private menuSupport;
    private wrapper;
    private drawer;
    private observer;
    private availableWidth;
    private requiredWidth;
    private scrollTop;
    private scrollDirection;
    private resizeThrottle;
    el: HTMLElement;
    isDrawer: boolean;
    isSupportDropdown: boolean;
    needSeparator: boolean;
    isMenuCreated: boolean;
    /**
     * O nome do sistema.
     */
    name: string;
    /**
     * Descrição do sistema. Usualmente, o nome do sistema (se usada a sigla em "name"), descrição ou um complemento ao nome do sistema.
     */
    description: string;
    /**
     * URL para a página inicial do sistema.
     */
    homeUrl: string;
    /**
     * Evento disparado quando o usuário aciona o link para a página inicial.
     * Executar `preventDefault()` evita que a navegação para a página inicial aconteça.
     */
    alcHome: EventEmitter<null>;
    handleScroll(): void;
    handleNavContent(event: CustomEvent<{
        navEl: HTMLAlcNavElement;
        isMobile: boolean;
    }>): void;
    handleResize(): void;
    private resize;
    private handleAlcHome;
    componentDidRender(): void;
    componentDidLoad(): void;
    render(): any;
    private calcRequiredWidth;
    private checkLayout;
    private fixedElementsVisible;
}
