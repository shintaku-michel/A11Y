import { EventEmitter } from '../../stencil-public-runtime';
export interface AlcActionDetail {
    originalEvent: MouseEvent | KeyboardEvent;
}
/**
 * @slot DEFAULT - Slot para o conteúdo do componente.
 */
export declare class AlcHeaderAction {
    hostElement: HTMLElement;
    slot: HTMLSlotElement;
    slotNodes: any;
    buttonContainer: HTMLElement;
    linkContainer: HTMLElement;
    menuItemContainer: HTMLElement;
    menuLinkContainer: HTMLElement;
    /**
     * Nome do ícone a ser exibido.
     */
    iconName: string;
    /**
     * Define o tipo de elemento a ser renderizado.
     */
    variant: 'button' | 'menu-item' | 'link' | 'menu-link';
    /**
     * URL para a página de suporte. Usado quando a propriedade `variant` é `link` ou `menu-link`.
     */
    url: string;
    private spaceKeyEvent;
    /**
     * Evento disparado quando o usuário aciona o suporte.
     */
    alcSelect: EventEmitter<{
        originalEvent: MouseEvent | KeyboardEvent;
    }>;
    /** handler comum a todas as ativações locais */
    private triggerEvent;
    handleKeyDownLink(event: KeyboardEvent): void;
    handleClickLink(event: MouseEvent): void;
    handleKeyDownButton(event: KeyboardEvent): void;
    handleClickButton(event: MouseEvent): void;
    handleAlcSelect(event: CustomEvent): void;
    componentDidLoad(): void;
    componentDidRender(): void;
    render(): any;
    renderButton(): any;
    renderLink(): any;
    renderMenuItem(): any;
    renderMenuLink(): any;
}
