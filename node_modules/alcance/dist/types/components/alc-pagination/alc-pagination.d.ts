import { EventEmitter } from '../../stencil-public-runtime';
type PageChangeType = 'isFirst' | 'isPrev' | 'isLast' | 'isNext' | 'isSelect';
export interface PageChangeEventProps {
    from: number;
    to: number;
    using: PageChangeType;
}
export declare class Pagination {
    private idSelectPagination;
    private disabledSelect;
    /**
     * Número total de páginas.
     */
    totalPages: number;
    watchPropTotalPages(newValue: number, oldValue: number): void;
    /**
     * Número da página atual
     */
    currentPage: number;
    /**
    * @type Array<number>
    * @default []
    *
    * Lista de páginas
    */
    pageList: Array<number>;
    /**
     * Disparado quando ocorre a mudança de uma página para outra.
     */
    alcChange: EventEmitter<{
        from: number;
        to: number;
        using: 'isFirst' | 'isPrev' | 'isLast' | 'isNext' | 'isSelect';
    }>;
    componentWillLoad(): void;
    private paginationItem;
    private checkPage;
    private handleSelectedPage;
    private handleChangePage;
    private pageChangeHandler;
    private renderPaginationItem;
    render(): any;
}
export {};
