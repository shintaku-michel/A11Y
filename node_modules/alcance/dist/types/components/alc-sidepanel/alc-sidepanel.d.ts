import { EventEmitter } from '../../stencil-public-runtime';
/**
 * @slot DEFAULT - Slot para o conteúdo do sidepanel.
 */
export declare class AlcSidepanel {
    el: HTMLAlcSidepanelElement;
    isMobile: boolean;
    drawer: HTMLAlcDrawerElement;
    slotContainer: HTMLElement;
    mobileSlot: HTMLElement;
    desktopSlot: HTMLElement;
    slotContent: HTMLElement;
    ro: ResizeObserver;
    state: {
        visible: boolean;
    };
    /**
     * Indica se o sidepanel está visível.
     */
    visible: boolean;
    /**
     * Abre o sidepanel.
     * @returns O valor retornado é `true` se o painel foi exibido com a chamada do método.
     */
    show(): Promise<boolean>;
    /**
     * Fecha o sidepanel
     * @returns O valor retornado é `true` se o painel foi dispensado com a chamada do método.
     */
    hide(): Promise<boolean>;
    handleResize(): void;
    /**
     * Evento disparado quando o estado do sidepanel é alterado.
     */
    alcStateChange: EventEmitter<{
        state: {
            visible: boolean;
        };
    }>;
    /**
     * Evento disparado quando o sidepanel precisa recuperar o estado.
     */
    alcStateRequest: EventEmitter<{
        state: {
            visible: boolean;
        };
    }>;
    componentWillLoad(): void;
    componentDidRender(): void;
    disconnectedCallback(): void;
    renderMobile: () => any[];
    render(): any;
    setOffset(): void;
}
