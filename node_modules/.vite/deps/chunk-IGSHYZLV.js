// node_modules/alcance/dist/esm-es5/theme-BR2kXvnz.js
function loadUserPreference() {
  return localStorage.getItem("alc-theme") || "system";
}
function saveUserPreference(e) {
  localStorage.setItem("alc-theme", e);
}
function removeUserPreference() {
  localStorage.removeItem("alc-theme");
}
function getAppliedTheme(e) {
  if (e === "light") {
    return "light";
  }
  if (e === "dark") {
    return "dark";
  }
  if (matchMedia("(prefers-color-scheme: dark)").matches) {
    return "dark";
  }
  return "light";
}
function setAppliedTheme(e) {
  var r = document.querySelector("html").dataset.alcThemeTag;
  r = r === void 0 ? "html" : r;
  if (r === "") {
    return;
  }
  var t = document.querySelectorAll(r);
  t.forEach(function(r2) {
    if (r2 instanceof HTMLElement) {
      r2.dataset["alcTheme"] = e;
    }
  });
}
function getMode() {
  var e = document.querySelector("html").dataset.alcNoTheme !== void 0;
  return e ? "base" : "theme";
}

export {
  loadUserPreference,
  saveUserPreference,
  removeUserPreference,
  getAppliedTheme,
  setAppliedTheme,
  getMode
};
//# sourceMappingURL=chunk-IGSHYZLV.js.map
